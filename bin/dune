(executable
 (name main)
 (public_name lexical_syntax_analysis)
 (libraries eval syntax lexer parser)
 (modes byte exe))

; (library
;  (name syntax)
;  (modules syntax))

; (ocamllex
;  (modules lexer))

; (ocamlyacc
;  (modules parser))
